machine:
  node:
    version: 5.0.0

test:
  override:
    - echo "test"

deployment:
  gitbooks:
    branch: master
    commands:
      - 'echo $CIRCLE_BUILD_NUM > spec/_version'
      - cd spec && npm install && npm run deploy

