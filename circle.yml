machine:
  node:
    version: 5.0.0

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install calibre

test:
  override:
    - echo "test"

deployment:
  gitbooks:
    branch: master
    commands:
      - 'echo $CIRCLE_BUILD_NUM > spec/_version'
      - cd spec && npm install && npm run deploy

