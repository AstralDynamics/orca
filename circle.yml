machine:
  node:
    version: 5.0.0

test:
  override:
    - echo "test"

deployment:
  gitbooks:
    branch: master
    commands:
      - cd reqs && npm install && npm run deploy
      - cd spec && npm install && npm run deploy

