machine:
  node:
    version: 5.0.0

deployment:
  requirements:
    branch: master
    commands:
      - cd reqs && npm install && npm run deploy
  spec:
    branch: master
    commands:
      - cd spec && npm install && npm run deploy

