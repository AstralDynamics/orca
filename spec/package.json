{
  "name": "nursing-spec",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "gitbook-cli": "^2.3.0",
    "surge": "^0.18.0",
    "svgexport": "^0.3.2"
  },
  "scripts": {
    "build": "gitbook build",
    "serve": "gitbook serve",
    "publish": "surge -d orcaspec.surge.sh _book",
    "prepdf": "mkdir -p _book",
    "pdf": "gitbook pdf ./ ./functional-spec.pdf",
    "postpdf": "mv ./functional-spec.pdf _book",
    "deploy": "npm run build && npm run pdf && npm run publish",
    "deploy-pdf": "npm run pdf && npm run publish",
    "deploy-html": "npm run build && npm run publish"
  },
  "keywords": [],
  "author": "Astral Dynamics"
}
