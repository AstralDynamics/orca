import { StyleSheet } from 'aphrodite'
import { colors } from '../constants/styles'

export default StyleSheet.create({
  hint: {
    fontSize: '.8rem'
    color: colors.grey
  },
  name: {
    fontSize: '.8rem',
    color: colors.grey,
    fontWeight: 'bold'
  }
})

